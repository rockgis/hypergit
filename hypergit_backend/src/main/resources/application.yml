server:
  servlet:
    session:
      #session delete when app restart
      persistent: false

saml:
  idp:
    authUrl: /sso
    entityId: hypergitSaml
    expired: 300
    clockSkew: 300

logging:
  level:
    root: info

---
spring:
  config:
    activate:
      on-profile: dev
server:
  port: 9090
---
spring:
  config:
    activate:
      on-profile: local
server:
  port: 10010
---